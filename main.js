var Tc = 100;
Tf = (9 / 5) * Tc + 32
alert(Tf)

var name = "Василий" 
var admin = null

admin = name

alert(admin)

var a = 1000 + "108"
alert(a)

// defer сообщает браузеру загружать скрипт в фоновом режиме, а затем запускать скрипт по очереди
// asunc браузер не ждёт загрузку скриптов, когда скрипт загрузился тутже открылся не зависит от порядка
